{
  "name": "cert-expiry-date",
  "version": "1.0.0",
  "description": "A website to let user query cert expiry date",
  "main": "This field intentionally left blank",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Xu Chunyang",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "check-cert-expiration": "^3.0.0"
  }
}
